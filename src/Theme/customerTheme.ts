import { createTheme } from "@mui/material";
const customerTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#01579b",
    },
    secondary: {
      main: "#1565c0",
    },
  },
});
export default customerTheme;
